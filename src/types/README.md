# Shared types

`database.ts` is generated from the deployed Supabase schema. Regenerate after schema changes. Runtime domain constraints are in `src/lib/domain.ts`; do not weaken the generated database types to make application code compile.
