# Feature boundary

Auth contains the sign-in/signup form and input validation. Keep reusable rendering and pure schemas here; compose server-only operations in app routes/actions and `src/server`. Add career workflows only in their authorized phases.
